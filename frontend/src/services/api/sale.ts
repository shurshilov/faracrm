/**
 * sale.ts — re-exports from canonical records.ts
 * @deprecated Import directly from '@/types/records' instead
 */
export { type SaleRecord as Sale } from '@/types/records';
export { type SaleLineRecord as SaleLine } from '@/types/records';
export type { SaleStageRecord } from '@/types/records';
