server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Специальное правило для вложений (100MB)
    # Ставим ПЕРЕД общим /api/, чтобы Nginx выбрал именно этот path
    location /api/attachments/ {
        client_max_body_size 100M;

        proxy_pass http://backend:8000/attachments/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # Рекомендуется увеличить таймаут для загрузки больших файлов
        proxy_read_timeout 300s;
        client_body_timeout 300s;
    }

    # API → backend (стандартные 1MB по умолчанию)
    location /api/ {
        proxy_pass http://backend:8000/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # WebSocket → backend
    location /ws/ {
        proxy_pass http://backend:8000/ws/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_read_timeout 86400;
    }

    # Developer docs → backend
    location /docs-dev/ {
        proxy_pass http://backend:8000/docs-dev/;
        proxy_set_header Host $host;
    }

    # SPA fallback — все остальные пути → index.html
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Cache static assets
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff2?)$ {
        expires 30d;
        add_header Cache-Control "public, immutable";
    }
}
